apiVersion: api.cerbos.dev/v1
principalPolicy:
  principal: "spiffe://shoracius.com.br/robot-financeiro"
  version: "default"
  rules:
    - resource: "nhi_api"
      actions:
        - action: "GET"
          effect: EFFECT_ALLOW
        - action: "HEAD"
          effect: EFFECT_ALLOW
        - action: "OPTIONS"
          effect: EFFECT_ALLOW
        - action: "POST"
          effect: EFFECT_DENY
        - action: "PUT"
          effect: EFFECT_DENY
        - action: "PATCH"
          effect: EFFECT_DENY
        - action: "DELETE"
          effect: EFFECT_DENY
